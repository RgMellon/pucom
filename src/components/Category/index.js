import React, { Component } from 'react';

import { ContainerCategory } from './styled';

class Category extends Component {
  state = {};

  render() {
    return <ContainerCategory />;
  }
}

export default Category;
